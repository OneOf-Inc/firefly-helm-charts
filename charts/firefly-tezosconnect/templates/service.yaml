apiVersion: v1
kind: Service
metadata:
  name: {{ include "firefly-tezosconnect.fullname" . }}
  labels:
    {{- include "firefly-tezosconnect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "firefly-tezosconnect.selectorLabels" . | nindent 4 }}
